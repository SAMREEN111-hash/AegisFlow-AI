spring:
  datasource:
    hikari:
      maximum-pool-size: ${DATABASE_POOL_MAX_SIZE:15}
      minimum-idle: ${DATABASE_POOL_MIN_IDLE:3}

management:
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    root: INFO
    com.aegisflow.api: INFO
